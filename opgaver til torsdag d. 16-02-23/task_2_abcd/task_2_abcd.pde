// Task2: Functions' return type.
// 2.a Look at the file TaskTwoA and fill out the missing line, using the happy boolean.

/*boolean happy = true;

 void setup() {
   if (iAmHappy())
   {
     println("I clap my hands");
   }
   else
   {
     println("I don't clap my hands"); 
   }
}

boolean iAmHappy(){
  if (happy) {
  return true;
  } else {
  return false;
  }
}  */


// 2.b Write a function that receives two integers as parameters and returns the sum of them.


/*void setup() {

  int result = add(2,3);
  println("The result is " + result);
}

  int add(int number1, int number2) {
    int sum = number1 + number2;
    return sum;
} */


// 2.c Write a function that receives a String and returns it as uppercase

/*void setup() {
 
  String originalString = "Hello Claire";
  String upperCaseString = toUpperCase(originalString);

  println(upperCaseString);
}

  String toUpperCase(String str) {
  return str.toUpperCase();
} */


// 2d. Write a function that receives a String and returns true if the first letter of the String is uppercase.
  
/*void setup() {
  String s = "Hello Claire";
  
  if (isFirstLetterUpperCase(s)) {
  println ("The first letter is uppercase");
  } else { 
  println("The first letter is not uppercase");
  }
}
  
  boolean isFirstLetterUpperCase(String s) {
  return Character.isUpperCase(s.charAt(0));
  } */
  
  
//2.e Call your new functions from setup(). In setup(), make sure the functions' return values are assigned to variables of the right data type. Print out the variables - still from setup.
  








 
